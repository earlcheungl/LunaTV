// 特殊源详情存根 - 精简版不启用
export function isSpecialSource(source: string): boolean {
  return false;
}

export async function getSpecialSourceDetail(source: string, id: string): Promise<any> {
  return null;
}
