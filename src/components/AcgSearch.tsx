// ACG 搜索存根 - 精简版不启用
'use client';

interface AcgSearchProps {
  query?: string;
  keyword?: string;
  triggerSearch?: boolean;
  onClose?: () => void;
  onError?: (error: any) => void;
}

export default function AcgSearch(props: AcgSearchProps) {
  return null;
}
