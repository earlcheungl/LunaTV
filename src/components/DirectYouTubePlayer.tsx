// YouTube 直接播放器存根 - 精简版不启用
'use client';

interface DirectYouTubePlayerProps {
  videoId: string;
  onClose: () => void;
}

export default function DirectYouTubePlayer(props: DirectYouTubePlayerProps) {
  return null;
}
